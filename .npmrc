# pnpm's strict, non-hoisted node_modules is a load-bearing architectural
# constraint here: a package can only import what its own package.json declares,
# so the layer boundaries in CLAUDE.md fail the build rather than only the lint.
#
# Do not enable shamefully-hoist or node-linker=hoisted.
